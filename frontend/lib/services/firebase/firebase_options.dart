// This file is a placeholder until `flutterfire configure` generates platform-specific options.
// Replace the dummy values with the generated configuration after running the command described
// in `firebase/setup.md`.

import 'package:firebase_core/firebase_core.dart';

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    return const FirebaseOptions(
      apiKey: 'DUMMY',
      appId: 'DUMMY',
      messagingSenderId: 'DUMMY',
      projectId: 'familycal-dev',
    );
  }
}
